apiVersion: v1
kind: Service
metadata:
  name: triage
  namespace: signal-desk
spec:
  type: NodePort
  selector: { app: triage }
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
